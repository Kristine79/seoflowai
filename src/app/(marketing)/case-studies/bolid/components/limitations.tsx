import { SectionHeading, BlueNote, NoteCard } from "./ui";

const LIMITATIONS = [
  "Это экспериментальный замер на фиксированном наборе промптов и провайдеров — не статистическое исследование рынка.",
  "Ответы AI нестабильны: повторные запуски могут отличаться без изменений бренда.",
  "Источники фиксируются только если провайдер вернул citations в ответе.",
  "Run #1 (chat, gpt-4o-mini) не сопоставим напрямую с web_search запусками.",
  "Классификация источников частично rule-based (детерминированная), официальные домены определяются по списку.",
  "Наблюдаемые изменения — это разница между запусками; причинность не утверждается.",
  "Гипотезы и действия в разделе «Гэпы → действия» требуют проверки повторным запуском.",
];

export function Limitations() {
  return (
    <section className="mx-auto max-w-5xl px-8 py-16 sm:py-20">
      <div className="space-y-10">
        <SectionHeading
          eyebrow="Limitations"
          title="Ограничения исследования"
        />
        <div className="space-y-3">
          {LIMITATIONS.map((l) => (
            <NoteCard key={l}>{l}</NoteCard>
          ))}
        </div>
        <BlueNote>
          Любая цифра в этом кейсе — наблюдение за ответами AI в рамках данного промпт-сета,
          а не утверждение о реальности бренда или рынка.
        </BlueNote>
      </div>
    </section>
  );
}